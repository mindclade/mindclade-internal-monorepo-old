# `mindclade_identifiers`

Validated names and Mindclade resource identifiers. `ResourceId` uses the same canonical representation as Go: `<kind>_<32 lowercase UUIDv7 hex characters>`. Cross-language fixture tests guard this wire identity. Generation is process-local and not an authorization primitive.
