# `mindclade_atomic_fs`

Root-confined relative paths, temporary-file publication, file and directory fsync, and
verified bounded reads. The API never accepts unvalidated absolute or traversal paths.
