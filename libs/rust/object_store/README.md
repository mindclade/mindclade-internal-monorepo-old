# `mindclade_object_store`

Stable conditional object-store contract with integrity-verifying in-memory and atomic
local-filesystem providers. Cloud SDK adapters live in owning services, not the shared
foundation, and must pass the same provider conformance suite.
