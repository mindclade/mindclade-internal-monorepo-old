# `mindclade_telemetry`

Bounded, deterministic, redaction-aware logs/events/traces/metrics contracts.
Export SDKs live in service adapters. The implementation is migrated from the
uploaded `observability` crate and retains its bounded attribute semantics.
