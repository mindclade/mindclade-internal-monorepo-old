# `mindclade_python_bridge`

Safe leaf bridge core for bounded parsers, digest/manifests, tokenization,
record decoding, and buffer descriptors. It does not embed Python. PyO3/ABI
adapters are leaf provider code and require explicit unsafe/sanitizer review.
