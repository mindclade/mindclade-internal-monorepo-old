# `mindclade_manifests`

Canonical node-local artifact identity and deterministic manifest validation.
`ArtifactRef` is the immutable identity used for dataset shards, checkpoints,
MSA/template outputs, evaluation results, model/runtime bundles, and release
evidence. Provider URI/generation belongs to `ArtifactLocation`.
