# `mindclade_record_io`

Versioned record framing with bounded allocation, canonical primitive encoding, SHA-256
integrity, and deterministic corruption behavior. It is used by checkpoint, streaming,
IPC, and telemetry persistence formats.
