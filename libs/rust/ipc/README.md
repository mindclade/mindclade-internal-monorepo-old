# `mindclade_ipc`

Versioned local worker protocol with major-version negotiation, bounded messages,
request identity, monotonic sequences, SHA-256 payload integrity, and deterministic
record framing. Transport sockets and authentication live in owning processes.
