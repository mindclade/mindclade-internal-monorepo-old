# `mindclade_tokenizer_runtime`

Small deterministic tokenizer mechanisms for byte streams, biological alphabets, and
fixed vocabularies. Training-specific tokenization policy and learned tokenizer assets
remain under `data/tokenizers/`.
