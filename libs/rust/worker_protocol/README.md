# `mindclade_worker_protocol`

Validated Rust views of the canonical runtime protobuf contract: execution
budgets/tickets, artifact grants, route snapshots, worker commands/status, and
bulk-data descriptors. Protobuf remains the cross-language wire authority;
these types enforce node-local invariants after decoding.
