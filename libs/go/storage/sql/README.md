# SQL adapters

`transaction` contains database/sql transaction execution. `postgres` contains
PostgreSQL-specific classification and pgx pool helpers. `sqltest` is a narrow
test driver for transaction boundary tests.
