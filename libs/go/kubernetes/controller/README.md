# Kubernetes controller middleware

Composable controller-runtime reconciler middleware for request lineage,
provider-error qualification, panic containment, and lifecycle observation.
Work queues, requeue policy, caches, watches, and worker concurrency remain
owned by controller-runtime.
