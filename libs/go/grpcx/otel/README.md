# Native gRPC OpenTelemetry adapter

Optional wrappers around the official `otelgrpc` client and server stats
handlers. Inject the returned handlers through `grpcx.ClientConfig` or
`grpcx.ServerConfig`.
