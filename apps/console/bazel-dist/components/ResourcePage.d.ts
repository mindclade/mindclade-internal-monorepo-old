import type { PublicResourceKind, ResourcePageCopy } from "../lib/types";
export declare function ResourcePage({ kind, copy }: {
    kind: PublicResourceKind;
    copy: ResourcePageCopy;
}): React.ReactNode;
