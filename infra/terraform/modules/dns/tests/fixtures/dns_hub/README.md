# Retired DNS hub fixture

This is a test-only composition of the reusable DNS module. Public zone state is owned by
`infrastructure-live`; this fixture deliberately has no backend, lock file, provider block,
credentials, bootstrap script, component registration, or apply instructions.
