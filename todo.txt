What's left

1. cloud.google.com/go/pubsub in go.mod (needs a connected environment) — then the memory branch goes back to tests; it's a one-function swap.
2. rules_go supporting Go 1.26 — until then Bazel compiles against 1.22.7 while go.mod pins 1.26.0, which is why httpx/middleware is the last failing target. Analysis is in MODULE.bazel.
3. Done. All twelve roles are materialized and every command links a real
   factory. admin reuses the api factory and operator reuses the controller's;
   ingestion-controller and maintenance are their own. cmd/maintenance did not
   exist and was created.
   Shared providers live in sibling packages (durable, broker, cluster, apikeys,
   objects) so a role links only what it uses.
   UNCONSUMED.toml is down to four entries: cursortest, workqueuetest,
   messaging/pubsub, and obstest.
   Open questions:
   - CONSUMPTION.md names maintenance as the migration process, but registry
     owns the manifest. Moving it is a deployment change; nothing runs two
     runners today.
   - coordination/projector has one intended consumer in the whole designed
     fleet, so it can never meet ADMISSION.md's two-consumer bar.
   - Every domain seam is still a fail-closed default: placement (scheduler),
     projection source and handler (event-projector), delivery (webhook),
     staging (ingestion), housekeeping (maintenance).
4. services/go_vanity/Dockerfile — the only failing architecture check, from the other session's commit b8a77af. Needs MINDCLADE_DEV_ONLY=1 or an OCI target. I left it alone.
