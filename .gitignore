# Copyright © 2026 Mindclade, LLC. All Rights Reserved.
# Mindclade Proprietary and Confidential.
# SPDX-License-Identifier: LicenseRef-Mindclade-Proprietary
#
# Note there is no bare `build/` rule. This tree uses `build/` as a source directory in
# several places — research/benchmarks/build/ and tools/build/ among them — so ignoring it
# by name would silently drop 129 tracked files from every future `git add`.

# --- Terraform ---
# .terraform/ is `terraform init` output — provider binaries for whichever platform ran it.
# Fifteen darwin_arm64 provider paths were tracked here, in a repository whose CI is Linux.
# .terraform.lock.hcl is the opposite case and stays: it pins provider versions and is meant
# to be committed, which is why it is excluded from the rule below rather than covered by it.
.terraform/
!.terraform.lock.hcl
.terragrunt-cache/
terraform.tfvars
*.auto.tfvars

# --- Rust ---
/target/
**/target/
*.rs.bk

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# --- Node / TypeScript ---
node_modules/
.pnpm-store/
*.tsbuildinfo
/apps/*/dist/
/apps/*/.next/
/sdk/typescript/dist/
/libs/ts/*/dist/

# --- Bazel ---
/bazel-*
/.bazel-cache/
user.bazelrc

# --- Generated documentation ---
/.generated/

# --- Buildkite runtime evidence ---
# The WIF canaries upload these JSON records as short-lived Buildkite artifacts. They contain
# no credentials, but they are run evidence rather than source and must never be committed.
/.buildkite-evidence/

# --- Go ---
/bin/
*.test
*.out

# --- Nix / direnv ---
/result
/result-*
.direnv/

# --- Agent scratch ---
# .claude/worktrees holds `git worktree` checkouts — each one a full copy of this repository,
# 300 MB and counting at the time this line was added. Untracked and unignored, they were one
# `git add -A` away from being committed, and tools that walk the filesystem rather than the
# index already had to learn about them: .bazelignore names the directory, and
# tools/analysis/check_go_modules.py skips it so every worktree's go.mod stops reading as an
# unexpected module boundary.
.claude/

# --- Local environment. Never commit these: they are the one place a real credential
#     plausibly lands in this repo. ---
.env
.env.*
!.env.example
*.pem
*.key
!**/testdata/**/*.pem
!**/testdata/**/*.key

# --- Editors and OS ---
.DS_Store
.idea/
.vscode/
!.vscode/extensions.json
*.swp
